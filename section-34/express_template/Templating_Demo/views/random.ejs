<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Random</title>
</head>

<body>
    <h1>Your random number is: <%= num %></h1>
    <% if(num % 2 === 0){ %>                    <!-- no equal sign. = embed JS without result added to template -->
    <h2>That is an even number!</h2>
    <% } else { %>                              <!-- not embedded b/c no equal sign-->
    <h2>That is an odd number!</h2>
    <% } %>
    <p>Here's an alternate way of doing it:</p>
    <h3>That number is: <%= num%2===0 ? 'EVEN' : 'ODD' %></h3> <!-- embedded b/c equal sign -->
</body>

</html>